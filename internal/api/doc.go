// Package api will expose the docker engine over HTTP: REST for lists and
// inspects, SSE for the event and stats streams, and a WebSocket for exec.
// It binds 127.0.0.1 by default. Introduced in Phase 4.
package api
